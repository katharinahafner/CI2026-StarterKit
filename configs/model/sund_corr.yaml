# @package _global_

model:
  _target_: starter_kit.baselines.sund_corr.SundMLPModel
  n_epochs: ${n_epochs}
  learning_rate: ${learning_rate}
  weight_decay: 0.
  best_threshold: 0.99
  log_csv: true

network:
  _target_: starter_kit.baselines.sund_corr.SundMLPNetwork
  input_dim: 30
  hidden_dim: 512
  n_layers: 4

# @package _global_

model:
  _target_: starter_kit.baselines.mlp_res.ResMLPModel
  n_epochs: ${n_epochs}
  learning_rate: ${learning_rate}
  weight_decay: 0.01
  best_threshold: 0.99
  log_csv: true

network:
  _target_: starter_kit.baselines.mlp_res.ResMLPNetwork
  input_dim: 40
  hidden_dim: 256
  n_layers: 6